# Anonymous Export

This directory is a sanitized file-level export intended for anonymous supplement upload. It excludes git history, remotes, per-query raw outputs, smoke-test artifacts, and local absolute paths.
